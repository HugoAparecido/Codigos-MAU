#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main()
{
    int n;
    while (n--)
    {
        string s;
        cin >> s;
        int ano = stoi(s);
        double raiz_quadrado = sqrt(ano);

        if (raiz_quadrada == (int)raiz_quadrado)
        {
            cout << -1 << endl;
        }
        else
        {
            int num1, num2;
            while
        }
    }

    return 0;
}